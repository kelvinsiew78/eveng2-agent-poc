# Optional. The token you enter in the Even app's "Add Agent" screen.
# Set it to enforce auth (requests must send "Authorization: Bearer <G2_TOKEN>").
# Leave it unset to accept any request (handy while poking at the protocol).
G2_TOKEN=

# Optional. Port the server listens on (default 3000).
PORT=3000
